import React from 'react';
import ListedProperties from '../components/user/ListedProperties';

const ListedPropertiesScreen = ({ navigation }) => {
  return (
    <ListedProperties navigation={navigation} />
  );
};


export default ListedPropertiesScreen;
